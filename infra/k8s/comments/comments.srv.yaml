apiVersion: v1
kind: Service
metadata:
  name: comments-srv-nodeport
spec:
  type: NodePort
  selector:
    app: comments
  ports:
    - port: 9999
      targetPort: 9999
      name: comments-port-nodeport
      protocol: TCP
