apiVersion: v1
kind: Service
metadata:
  name: query-srv-nodeport
spec:
  type: NodePort
  selector:
    app: query
  ports:
    - port: 9998
      targetPort: 9998
      name: query-port-nodeport
      protocol: TCP
