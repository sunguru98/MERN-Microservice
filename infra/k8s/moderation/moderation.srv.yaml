apiVersion: v1
kind: Service
metadata:
  name: moderation-srv-nodeport
spec:
  type: NodePort
  selector:
    app: moderation
  ports:
    - port: 9998
      targetPort: 9998
      name: moderation-port-nodeport
      protocol: TCP
